get_mem_info:

            ;レジスタの保存
            push    eax
            push    ebx
            push    ecx
            push    edx
            push    si
            push    di
            push    bp
            